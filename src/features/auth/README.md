# Auth Feature - AgroGuard X

This folder will contain the production-ready frontend authentication module:

- screens (splash, welcome, language, login, register, otp, forgot/reset password, role selection)
- API layer + repository pattern (axios)
- Auth context + hooks
- secure token storage (expo-secure-store)
- authentication guards/protected routing (expo-router integration)
